import React from 'react';
import './Loading.css';

function Loading() {
    return(
        <div className="container">
            <div className="loadingBar" />
        </div>
    );
}

export default Loading;